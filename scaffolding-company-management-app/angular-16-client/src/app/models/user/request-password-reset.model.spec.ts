import {RequestPasswordReset} from './request-password-reset.model';

describe('RequestPasswordReset', () => {
  it('should create an instance', () => {
    expect(new RequestPasswordReset()).toBeTruthy();
  });
});
